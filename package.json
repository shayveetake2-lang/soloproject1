{
  "name": "veloce",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "npm run build",
    "preview": "vite preview",
    "deploy": "npx wrangler pages deploy",
    "deploy:pages": "npx wrangler pages deploy dist --project-name veloce --branch main --commit-dirty=true",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules"
  },
  "dependencies": {
    "firebase": "^11.10.0",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5",
    "wrangler": "^4.126.0"
  }
}
